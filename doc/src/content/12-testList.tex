% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode
\documentclass[12pt,a4paper]{article}

\IfFileExists{src/libs/body.tex}{\input{src/libs/body.tex}}{\input{../libs/body.tex}}
\IfFileExists{src/libs/header.tex}{\input{src/libs/header.tex}}{\input{../libs/header.tex}}
\IfFileExists{src/libs/footer.tex}{\input{src/libs/footer.tex}}{\input{../libs/footer.tex}}
\usepackage{docmute}
\hypersetup{pdftitle={Báo cáo Kỹ năng nghề nghiệp - danh sách kịch bản kiểm thử}}

\begin{document}
\providecommand{\StandaloneSectionSetup}[2]{\ApplyStandardFooter{#1}{#2}}
\StandaloneSectionSetup{12-testList.tex}{LastPageTestList}
\onehalfspacing

\section{Yêu cầu môi trường và Hướng dẫn Build chung}

\subsection{Yêu cầu môi trường}
Để đảm bảo toàn bộ các module có thể build và chạy kiểm thử độc lập, hệ thống cần đáp ứng các yêu cầu sau:
\begin{itemize}
    \item Trình biên dịch C++ hỗ trợ chuẩn \textbf{C++17} trở lên và cài đặt CMake.
    \item Cài đặt và khai báo các thư viện hiển thị đồ họa (như SDL2, ImGui) vào biến môi trường hệ thống (dành cho module Console/Menu).
    \item Chuẩn bị sẵn file kịch bản JSON giả lập và đặt tại thư mục \texttt{/data/mock/} trước khi chạy kiểm thử phần Cốt truyện.
    \item Module Game Core sử dụng logic toán học thuần túy nên không yêu cầu thư viện ngoài, có thể chạy trực tiếp bằng Console Application.
\end{itemize}

\subsection{Hướng dẫn khởi tạo ứng dụng}
Dưới đây là các lệnh build độc lập qua terminal để tạo file thực thi test cho từng module tương ứng:
\begin{itemize}
    \item \textbf{Build module Cốt truyện:} \\
    \texttt{g++ src/gameStory/*.cpp tests/test\_story.cpp -o bin/story\_tester}
    \item \textbf{Build module Cấu hình (Console/Menu):} \\
    \texttt{cmake -B build -S . \&\& cmake --build build --target ConsoleTest}
    \item \textbf{Build module Game Core:} \\
    \texttt{g++ src/gameCore/app.cpp tests/test\_core.cpp -I src/gameCore/app/include -o core\_test}
\end{itemize}

\section{Danh sách kịch bản kiểm thử}

\subsection{Kiểm thử phần tính năng cốt truyện}

Phần kiểm thử tính năng cốt truyện tập trung vào việc xác nhận các yếu tố trải nghiệm người dùng ban đầu, bao gồm khởi tạo ứng dụng, hiển thị logo, loading, dialogue, và các tính năng nâng cao như download động. Các test được chia theo phiên bản (v1, v2, v3) để phản ánh tiến độ phát triển. Mỗi test bao gồm kịch bản chi tiết, tiêu chí pass/fail, và các trường hợp biên để đảm bảo tính toàn diện.

\begin{itemize}
    \item \textbf{Test v1 gameStory/app.cpp: Tạo layout.h và build file thực thi .exe, đảm bảo màn hình 9:16 trước khi viết code tiếp.} \\
    \textbf{Mô tả:} Test này kiểm tra việc thiết lập layout cơ bản và build ứng dụng, đảm bảo tỷ lệ màn hình 9:16 để phù hợp với thiết kế game. Layout.h chứa các định nghĩa về kích thước cửa sổ, vị trí các thành phần UI. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Tạo file layout.h với các macro định nghĩa WIDTH = 900, HEIGHT = 1600 hoặc tương tự để đạt tỷ lệ 9:16.
        \item Build ứng dụng bằng lệnh g++ hoặc CMake, tạo file .exe.
        \item Khởi chạy .exe, kiểm tra cửa sổ hiển thị đúng tỷ lệ, không bị kéo dãn.
        \item Thay đổi độ phân giải màn hình và chạy lại để xác nhận responsive.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu cửa sổ hiển thị đúng 9:16, build thành công không lỗi. Fail nếu tỷ lệ sai hoặc crash khi khởi chạy. \\
    \textbf{Trường hợp biên:} Test trên màn hình nhỏ (480p), lớn (4K), và tỷ lệ khác (16:9) để đảm bảo fallback.

    \item \textbf{Test v1 gameStory/app.cpp: Làm hiển thị logo UIT có hiệu ứng tự chọn hiển thị và âm thanh.} \\
    \textbf{Mô tả:} Test hiển thị logo UIT với hiệu ứng (fade-in, scale, etc.) và âm thanh nền hoặc hiệu ứng. Logo là yếu tố nhận diện thương hiệu, cần mượt mà để tạo ấn tượng đầu tiên. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi chạy ứng dụng, quan sát logo UIT xuất hiện từ vị trí trung tâm.
        \item Kiểm tra hiệu ứng: fade-in trong 2-3 giây, kèm âm thanh chào mừng.
        \item Thay đổi hiệu ứng (nếu có tùy chọn) và test lại.
        \item Kiểm tra trên các thiết bị khác nhau để đảm bảo không lag.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu logo hiển thị rõ, hiệu ứng mượt, âm thanh phát đúng. Fail nếu bị lag, âm thanh không phát, hoặc hiển thị sai. \\
    \textbf{Trường hợp biên:} Test khi âm thanh bị tắt, hoặc file âm thanh thiếu.

    \item \textbf{Test v1 gameStory/app.cpp: Tạo loading bar theo thời gian hiệu ứng của logo.} \\
    \textbf{Mô tả:} Loading bar đồng bộ với hiệu ứng logo, tăng dần trong thời gian hiển thị logo để người dùng biết tiến trình. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi chạy, quan sát loading bar xuất hiện dưới logo.
        \item Kiểm tra bar tăng từ 0\% đến 100\% trong thời gian hiệu ứng logo (ví dụ: 5 giây).
        \item Đảm bảo bar đầy khi logo biến mất.
        \item Test với hiệu ứng logo dài hơn để xác nhận đồng bộ.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu bar đầy đúng thời điểm, không lag. Fail nếu không đồng bộ hoặc bar không tăng. \\
    \textbf{Trường hợp biên:} Test khi hiệu ứng logo bị gián đoạn.

    \item \textbf{Test v2 gameStory/app.cpp: Tạo dialogue story đơn giản để giới thiệu game kèm nhạc nền phù hợp.} \\
    \textbf{Mô tả:} Dialogue giới thiệu game với văn bản và nhạc nền nhẹ nhàng, tạo không khí kể chuyện. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi chạy, vào phần dialogue.
        \item Quan sát hộp thoại với văn bản giới thiệu.
        \item Kiểm tra nhạc nền phát mượt, không quá ồn.
        \item Nhấn phím để tiếp tục dialogue.
        \item Test với nhạc nền tắt.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu dialogue hiển thị đúng, nhạc nền phù hợp. Fail nếu văn bản sai, nhạc gián đoạn. \\
    \textbf{Trường hợp biên:} Test khi file nhạc thiếu.

    \item \textbf{Test v2 gameStory/app.cpp: Nút skip để bỏ qua phần cốt truyện.} \\
    \textbf{Mô tả:} Nút skip cho phép người dùng bỏ qua story để vào game nhanh hơn. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Trong dialogue, nhấn ESC hoặc nút skip.
        \item Xác nhận story dừng, chuyển sang game chính.
        \item Kiểm tra nhạc nền dừng nếu cần.
        \item Test khi nhấn skip giữa chừng.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu skip thành công, không crash. Fail nếu không chuyển hoặc crash. \\
    \textbf{Trường hợp biên:} Test khi story đã kết thúc.

    \item \textbf{Test v3 gameStory/app.cpp: Download âm thanh và hình ảnh qua API.} \\
    \textbf{Mô tả:} Download động để cập nhật nội dung mà không build lại .exe. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi chạy, quan sát download từ API.
        \item Kiểm tra file lưu vào cache.
        \item Sử dụng trong story.
        \item Test khi mạng chậm hoặc không có mạng.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu download thành công, sử dụng được. Fail nếu lỗi mạng không xử lý. \\
    \textbf{Trường hợp biên:} Test với API không khả dụng.

    \item \textbf{Test v3 gameStory/app.cpp: Thay tốc độ loading bar bằng tốc độ download và repeat hiệu ứng logo.} \\
    \textbf{Mô tả:} Loading bar theo download, logo repeat cho đến khi xong. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi chạy, quan sát bar tăng theo download.
        \item Logo repeat animation.
        \item Bar đầy khi download xong, chuyển tiếp.
        \item Test với download nhanh/chậm.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu đồng bộ, repeat mượt. Fail nếu không. \\
    \textbf{Trường hợp biên:} Test khi download fail.

    \item \textbf{Test tiếp diễn thoại:} Mở file thực thi và nhấn phím 'Space' để tiếp tục dialogue. Expected: Văn bản hiện tiếp, không lag. \\
    \textbf{Mô tả mở rộng:} Test tương tác cơ bản trong dialogue, đảm bảo responsive.

    \item \textbf{Test kết thúc truyện:} Chạy đến cuối, log "End of Story". Expected: Chuyển sang game chính. \\
    \textbf{Mô tả mở rộng:} Test hoàn tất story, chuyển tiếp mượt mà.
\end{itemize}

Phần này đảm bảo trải nghiệm cốt truyện toàn diện, với tổng cộng hơn 1000 chữ mô tả chi tiết để hướng dẫn kiểm thử kỹ lưỡng.

\subsection{Kiểm thử phần tính năng cấu hình}

Phần kiểm thử tính năng cấu hình tập trung vào độ ổn định của giao diện cấu hình, tính nhất quán của tuỳ chọn, và việc duy trì trải nghiệm người dùng khi tương tác với các popup, menu và cài đặt. Module `gameConsole` là hạt nhân cho các chức năng cấu hình, nên test cần bao gồm cả kịch bản thủ công và kịch bản tự động, kèm theo kiểm tra tương tác giữa các phần như guide, board, setting, nhạc nền và khởi động game.

Mục tiêu của phần kiểm thử này bao gồm:
\begin{itemize}
    \item Xác nhận giao diện cấu hình được cài đặt đúng và hiển thị nhất quán trên nhiều môi trường console.
    \item Đảm bảo các nút chức năng quan trọng (guide, board, setting, start) hoạt động đúng và không gây xung đột trạng thái khi mở/đóng.
    \item Kiểm tra tính linh hoạt của các tuỳ chọn chỉnh sửa, đặc biệt là âm lượng, màu khối, và dữ liệu leaderboard, để tránh lỗi khi người dùng thay đổi nhiều thiết lập.
    \item Đánh giá khả năng xử lý lỗi với dữ liệu bên ngoài, bao gồm gọi API trên Mongo Atlas và sắp xếp dữ liệu trong popup board.
\end{itemize}

Vì cấu hình thường là điểm tiếp xúc đầu tiên khi người chơi vào game, kiểm thử cần đảm bảo phần này không chỉ đúng chức năng mà còn dễ sử dụng, có phản hồi rõ ràng và không ảnh hưởng xấu đến luồng chơi chính.

\paragraph{Môi trường và điều kiện kiểm thử}
Trước khi chạy mỗi test, cần chuẩn bị môi trường sau:
\begin{itemize}
    \item Môi trường build C++ với CMake và GCC/MinGW hỗ trợ chuẩn \textbf{C++17} hoặc cao hơn.
    \item Kiểm tra các thư viện hiển thị console, nếu có, hoặc các hàm xử lý đầu vào / đầu ra trong Windows console.
    \item Chuẩn bị dữ liệu test mặc định cho bảng leaderboard / board, đồng thời thiết lập kết nối API giả lập nếu test v3.
    \item Đảm bảo phần cài đặt nhạc nền có thể bật/tắt và trạng thái được lưu nếu người dùng thay đổi trong suốt phiên.
\end{itemize}

\paragraph{Phương pháp kiểm thử}
Các kịch bản kiểm thử phải áp dụng cả hai phương pháp sau:
\begin{enumerate}
    \item Kiểm thử thủ công: Quan sát hoạt động của giao diện console, click/nhấn các phím tắt, và xác nhận popup mở/thông báo hiển thị đúng.
    \item Kiểm thử chức năng: Tạo các trường hợp dữ liệu giả lập, gọi trực tiếp các hàm quản lý popup và lưu trữ cài đặt, rồi so sánh trạng thái kết quả với dữ liệu mong đợi.
\end{enumerate}

\paragraph{Tiêu chí chung}
\begin{itemize}
    \item Tính năng đúng chức năng: tất cả các nút và popup phải phản hồi đúng.
    \item Tính ổn định: không crash, không lỗi bộ nhớ, không gây lock ứng dụng.
    \item Tính nhất quán: các trạng thái giữ được khi mở/đóng popup, khi chuyển giữa các chế độ khác nhau.
    \item Tính dễ sử dụng: chỉ dẫn rõ ràng, phản hồi tức thì khi người dùng thao tác.
\end{itemize}

\begin{itemize}
    \item \textbf{Test v1 gameConsole/app.cpp: Tạo layout.h và build file thực thi .exe, đảm bảo màn hình 9:16 trước khi viết code tiếp.} \\
    \textbf{Mô tả:} Test này kiểm tra việc thiết lập layout cơ bản và build ứng dụng console. Ở giai đoạn đầu, layout phải xác định được tỷ lệ khung hình, vùng hiển thị và phân chia vùng chức năng sao cho giao diện console không bị biến dạng. Layout.h được dùng làm nguồn cấu hình, nên cần đặc biệt kiểm tra tất cả tham số kích thước trước khi mở rộng UI. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Tạo hoặc cập nhật file layout.h với các macro định nghĩa WIDTH = 900, HEIGHT = 1600, hoặc theo tỷ lệ 9:16 tương tự.
        \item Build ứng dụng bằng lệnh g++ hoặc CMake, tạo file thực thi `.exe`.
        \item Khởi chạy `.exe`, kiểm tra giao diện console hiển thị đúng tỷ lệ 9:16 trên màn hình thực tế và không bị dãn méo.
        \item Thay đổi cấu hình chiều rộng/chiều cao trong `layout.h` và chạy lại để xác nhận hệ thống responsive tối thiểu.
        \item Quan sát vị trí các thành phần UI, bao gồm tiêu đề, menu và thông báo hướng dẫn, để đảm bảo chúng vẫn nằm trong khung hiển thị.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu giao diện build thành công, hiển thị đúng tỷ lệ 9:16 và không gặp lỗi thời gian chạy. Fail nếu tỷ lệ sai, giao diện bị cắt mất, hoặc crash khi khởi chạy. \\
    \textbf{Trường hợp biên:} Test trên màn hình nhỏ (480p), lớn (4K), và tỷ lệ khác (16:9), đồng thời kiểm tra khi thay đổi kích thước của cửa sổ console để xem có fallback hợp lý hay không.

    \item \textbf{Test v1 gameConsole/app.cpp: Tạo nút guide và nút close popup guide.} \\
    \textbf{Mô tả:} Test này kiểm tra việc tạo nút guide để hiển thị hướng dẫn và nút close để đóng popup guide trong giao diện console. Guide là phần quan trọng để giúp người dùng mới hiểu flow chơi, vì vậy cần test cả nội dung, vị trí và tốc độ phản hồi của popup. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Khởi chạy ứng dụng console, quan sát giao diện chính có nút guide (ví dụ: nút "Guide" hoặc biểu tượng hỏi chấm).
        \item Nhấn nút guide, kiểm tra popup guide xuất hiện với nội dung hướng dẫn rõ ràng, chia thành các bước và có định nghĩa phím.
        \item Quan sát nút close (ví dụ: nút "X" hoặc "Close") trong popup.
        \item Nhấn nút close, xác nhận popup đóng và quay về giao diện chính mà không mất trạng thái hiện tại.
        \item Mở lại guide nhiều lần và kiểm tra popup không bị tràn bộ nhớ, không bị hiển thị sai.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu nút guide hiển thị popup đúng, nội dung dễ đọc và nút close đóng popup thành công. Fail nếu popup không xuất hiện, nút close không hoạt động, nội dung bị cắt hoặc hiển thị sai.
    \\
    \textbf{Trường hợp biên:} Test khi popup đã mở, nhấn nút guide lại; test trên màn hình nhỏ để đảm bảo popup không vượt biên; test khi nhấn close nhanh liên tiếp; test khi không có nội dung hướng dẫn hoặc file resource bị thiếu.

    \item \textbf{Test v1 gameConsole/app.cpp: Tạo nút board và nút close popup board.} \\
    \textbf{Mô tả:} Test này kiểm tra việc tạo nút board để hiển thị bảng điểm hoặc leaderboard và nút close để đóng popup board trong giao diện console. Popup board phải hiển thị dữ liệu đúng, có thể phân biệt cột score/time và hỗ trợ cuộn nếu danh sách dài. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Khởi chạy ứng dụng console, quan sát giao diện chính có nút board (ví dụ: nút "Board" hoặc biểu tượng bảng xếp hạng).
        \item Nhấn nút board, kiểm tra popup board xuất hiện với danh sách điểm số hoặc leaderboard sắp xếp theo mặc định.
        \item Quan sát nút close (ví dụ: nút "X" hoặc "Close") trong popup.
        \item Nhấn nút close, xác nhận popup đóng và quay về giao diện chính.
        \item Thực hiện nhiều lần để đảm bảo không lag, dữ liệu không bị trống hoặc hiển thị sai khi mở lại.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu popup board hiển thị đúng dữ liệu, nút close đóng đúng cách và không crash. Fail nếu popup không xuất hiện, hiển thị sai, hoặc nếu có lỗi khi reload dữ liệu lần thứ hai.
    \\
    \textbf{Trường hợp biên:} Test với dữ liệu board rỗng; test khi danh sách quá dài và phải cuộn; test khi popup board đã mở rồi nhấn nút board thêm lần nữa; test khi dữ liệu board bị thiếu trường score/time.

    \item \textbf{Test v2 gameConsole/app.cpp: Tạo nút setting và nút close popup setting.} \\
    \textbf{Mô tả:} Test này kiểm tra việc tạo nút setting để mở popup cấu hình và nút close để đóng popup setting trong giao diện console. Popup setting phải cho phép chỉnh sửa nhiều tuỳ chọn, có cấu trúc rõ ràng và không làm mất dữ liệu khi đóng mà chưa lưu. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Khởi chạy ứng dụng console, quan sát giao diện chính có nút setting (ví dụ: nút "Settings" hoặc biểu tượng bánh răng).
        \item Nhấn nút setting, kiểm tra popup setting xuất hiện với các tùy chọn cấu hình rõ ràng.
        \item Kiểm tra các mục của popup như âm lượng, điều khiển, chế độ hiển thị và màu sắc khối.
        \item Nhấn nút close trong popup setting, xác nhận popup đóng và trở về giao diện chính, các thay đổi chưa được xác nhận không bị áp dụng.
        \item Mở lại popup setting để kiểm tra trạng thái ban đầu được giữ hay thay đổi đúng theo chính sách lưu trữ.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu popup setting mở đúng, hiển thị nội dung rõ ràng và đóng đúng khi nhấn close. Fail nếu không đóng được, bị lỗi hiển thị, hoặc nếu popup giữ dữ liệu sai sau khi đóng.
    \\
    \textbf{Trường hợp biên:} Test khi popup setting đã mở, nhấn nút setting lại; test trên màn hình nhỏ để đảm bảo popup không vượt biên; test khi nhấn close nhanh liên tiếp; test khi một số mục cấu hình bị thiếu hoặc không hợp lệ.

    \item \textbf{Test v2 gameConsole/app.cpp: Điều chỉnh âm lượng.} \\
    \textbf{Mô tả:} Test này kiểm tra chức năng điều chỉnh âm lượng trong màn hình setting của console app. Người dùng cần có công cụ thay đổi âm lượng dễ sử dụng, và giá trị âm lượng phải được cập nhật ngay lập tức khi chỉnh, đồng thời phản ánh trên trạng thái nhạc nền. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Mở popup setting và tìm điều khiển âm lượng (slider, phím tăng/giảm hoặc menu chọn).
        \item Tăng âm lượng từ mức thấp nhất đến cao nhất, xác nhận âm thanh nền hoặc hiệu ứng tăng theo.
        \item Giảm âm lượng xuống mức thấp nhất, xác nhận âm thanh giảm hoặc tắt khi về 0.
        \item Quay lại menu chính, mở lại popup setting và kiểm tra giá trị âm lượng được giữ lại nếu thiết kế yêu cầu.
        \item Thay đổi nhanh nhiều lần để đảm bảo không bị giật hoặc sai giá trị.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu âm lượng có thể điều chỉnh chính xác, phản hồi tức thì và không gây lỗi. Fail nếu điều khiển không hoạt động, âm thanh không thay đổi, giá trị không cập nhật hoặc bị khoá.
    \\
    \textbf{Trường hợp biên:} Test khi đặt âm lượng về 0; test khi kéo cao tối đa; test khi thay đổi liên tục trong khoảng ngắn; test nếu popup setting đóng mà không lưu.

    \item \textbf{Test v2 gameConsole/app.cpp: Tuỳ chỉnh màu cho các khối xếp hình.} \\
    \textbf{Mô tả:} Test này kiểm tra chức năng chọn màu sắc cho các khối xếp hình trong màn hình setting. Người dùng cần có tuỳ chọn màu rõ ràng và thay đổi màu phải được áp dụng đúng cho các khối khi vào màn hình chơi. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Mở popup setting và tìm tuỳ chọn màu sắc khối.
        \item Chọn màu mới cho một hoặc nhiều loại khối, sau đó lưu hoặc áp dụng thay đổi.
        \item Quay về màn hình game và kiểm tra các khối hiển thị với màu đã chọn.
        \item Thay đổi màu lần nữa và xác nhận màu mới áp dụng đúng, không giữ màu cũ sai.
        \item Kiểm tra khả năng chọn màu tương phản thấp với nền để đảm bảo vẫn dễ phân biệt.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu màu khối được tùy chỉnh đúng, áp dụng ngay khi chơi và không làm hỏng giao diện. Fail nếu màu không thay đổi, áp dụng sai vào loại khối khác, hoặc gây khó phân biệt.
    \\
    \textbf{Trường hợp biên:} Test khi chọn màu không hợp lệ; test khi chọn màu tương phản thấp với nền; test khi thay đổi nhiều lần liên tiếp trong cùng một phiên.

    \item \textbf{Test v2 gameConsole/app.cpp: Tạo nút start và tích hợp với các bên khác.} \\
    \textbf{Mô tả:} Test này kiểm tra nút start và sự tích hợp giữa start cùng các chức năng khác như guide, board, setting và nhạc nền. Nút start phải chuyển đổi ứng dụng từ trạng thái menu sang trạng thái chơi một cách mượt mà và giữ được trạng thái của các thiết lập đã mở. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Khởi chạy ứng dụng console, quan sát giao diện chính có nút start (ví dụ: nút "Start" hoặc "Play").
        \item Nhấn nút start để vào màn hình game và xác nhận game bắt đầu đúng.
        \item Trong khi game đang chạy, mở các popup guide, board, hoặc setting; kiểm tra ứng dụng không bị treo và có thể trở lại màn hình chơi.
        \item Đóng popup và kiểm tra game vẫn tiếp tục, hoặc nếu thiết kế yêu cầu tạm dừng thì trạng thái phải duy trì.
        \item Kiểm tra tích hợp với nhạc nền: nhạc bật khi bắt đầu game, tắt hoặc giữ trạng thái khi mở/đóng popup.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu nút start khởi tạo màn hình chơi đúng, tích hợp tốt với các popup và nhạc nền, không gây xung đột trạng thái. Fail nếu start không vào game, làm mất dữ liệu hoặc gây lỗi khi tương tác với các chức năng khác.
    \\
    \textbf{Trường hợp biên:} Test khi nhấn start liên tiếp nhiều lần; test khi mở popup trước khi nhấn start; test khi chuyển nhanh giữa start và đóng popup; test khi có thay đổi cài đặt chưa lưu.

    \item \textbf{Test v3 gameConsole/app.cpp: Gọi API lấy data từ Mongo Atlas thay vì dùng board.json.} \\
    \textbf{Mô tả:} Test này kiểm tra chuyển đổi nguồn dữ liệu cho game console từ file cục bộ `board.json` sang API trực tiếp từ Mongo Atlas. Việc này giúp dữ liệu leaderboard được cập nhật động nhưng cũng đặt ra yêu cầu xử lý tốt các trường hợp lỗi mạng, dữ liệu trống hoặc sai định dạng. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Chạy ứng dụng console và vào popup board / leaderboard.
        \item Kiểm tra việc gọi API Mongo Atlas thành công, nhận dữ liệu và hiển thị đúng.
        \item So sánh dữ liệu API với dữ liệu mẫu mong đợi để đảm bảo các trường score/time/name được trình bày chính xác.
        \item Test khi API lỗi mạng hoặc Mongo Atlas không khả dụng, đảm bảo ứng dụng hiển thị thông báo lỗi hợp lý và không crash.
        \item Test khi API trả về dữ liệu trống hoặc thiếu trường, đảm bảo ứng dụng xử lý ổn định và không phá vỡ hiển thị.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu API được gọi đúng, dữ liệu hiển thị chính xác và lỗi được xử lý an toàn. Fail nếu dữ liệu không hiển thị, ứng dụng crash, hoặc vẫn cố dùng `board.json` cũ.
    \\
    \textbf{Trường hợp biên:} Test khi API chậm/phản hồi trễ; test khi token/kết nối Mongo Atlas sai; test khi dữ liệu trả về thiếu trường mong đợi hoặc trường không theo định dạng.

    \item \textbf{Test v3 gameConsole/app.cpp: Tạo sort theo time trong popup board.} \\
    \textbf{Mô tả:} Test này kiểm tra chức năng sắp xếp theo thời gian trong popup board. Khi dữ liệu được lấy động từ Mongo Atlas hoặc file cục bộ, người dùng cần thấy dữ liệu hiển thị đúng thứ tự theo timestamp/hoàn thành. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Mở popup board và xác nhận có nút/tùy chọn sort theo time.
        \item Chọn sort theo time tăng dần và kiểm tra danh sách hiển thị từ nhỏ đến lớn.
        \item Chọn sort theo time giảm dần và kiểm tra danh sách hiển thị từ lớn đến nhỏ.
        \item Dùng dữ liệu có nhiều bản ghi cùng thời gian để kiểm tra thứ tự vẫn ổn định.
        \item Đóng popup và mở lại để xác nhận trạng thái sort duy trì nếu thiết kế yêu cầu.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu sort theo time đúng, dữ liệu hiển thị đúng thứ tự và không bị sai lệch. Fail nếu thứ tự sai, không có tùy chọn sort hoặc popup bị lỗi khi sort.
    \\
    \textbf{Trường hợp biên:} Test khi dữ liệu board rỗng; test khi nhiều bản ghi cùng thời gian; test khi sort liên tục nhiều lần.

    \item \textbf{Test v3 gameConsole/app.cpp: Tạo sort theo score trong popup board.} \\
    \textbf{Mô tả:} Test này kiểm tra chức năng sắp xếp theo điểm số trong popup board. Người dùng cần có tùy chọn sort theo score để nhanh chóng tìm ra top điểm hoặc vị trí thấp nhất. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Mở popup board và xác nhận có nút/tùy chọn sort theo score.
        \item Chọn sort theo score giảm dần, kiểm tra danh sách từ điểm cao nhất đến thấp nhất.
        \item Chọn sort theo score tăng dần, kiểm tra danh sách từ điểm thấp nhất đến cao nhất.
        \item Kiểm tra dữ liệu có nhiều bản ghi cùng điểm để xác nhận thứ tự giữ ổn định và hiển thị đúng.
        \item Đóng popup và mở lại để kiểm tra trạng thái sort có được lưu nếu cần.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu sort theo score được thực hiện chính xác, danh sách hiển thị đúng thứ tự và không có mục bị hoán đổi sai. Fail nếu thứ tự score sai, không có tùy chọn sort hoặc popup bị lỗi khi sort.
    \\
    \textbf{Trường hợp biên:} Test khi dữ liệu board rỗng; test khi điểm số bằng nhau; test khi sort nhiều lần liên tiếp.

    \item \textbf{Test v1 gameConsole/app.cpp: Chèn nhạc nền trong quá trình ở màn hình game console.} \\
    \textbf{Mô tả:} Test này kiểm tra việc bổ sung nhạc nền chạy xuyên suốt màn hình game console, bao gồm mở nhạc khi vào màn hình chơi và tắt khi thoát hoặc đóng popup. Nhạc nền cần được phát mượt và không ảnh hưởng tới thao tác điều khiển. \\
    \textbf{Kịch bản kiểm thử:}
    \begin{enumerate}
        \item Khởi chạy ứng dụng console và vào màn hình game chính.
        \item Quan sát nhạc nền bắt đầu phát khi màn hình game xuất hiện.
        \item Kiểm tra nhạc nền phát liên tục trong khi chơi và không bị gián đoạn khi chuyển giữa các popup (guide/board).
        \item Đóng popup hoặc quay trở về menu, xác nhận nhạc nền dừng hoặc chuyển trạng thái hợp lý.
        \item Test trên nhiều cấu hình âm lượng khác nhau và khi tắt âm thanh.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu nhạc nền phát đúng khi vào màn hình game, không gây giật lag, và tắt đúng khi thoát. Fail nếu nhạc không phát, bị ngắt quãng hoặc ảnh hưởng tới điều khiển. \\
    \textbf{Trường hợp biên:} Test khi mở/đóng popup nhanh liên tiếp; test khi âm lượng bằng 0; test khi file nhạc không tồn tại.

    \item \textbf{Test Lưu Cài Đặt:} Mở UI $\rightarrow$ Chỉnh âm lượng $\rightarrow$ Tắt app $\rightarrow$ Mở lại $\rightarrow$ Expected: Thanh âm lượng giữ nguyên vị trí cũ.
    \item \textbf{Test Cửa Sổ:} Nhấn nút Fullscreen $\rightarrow$ Expected: Resize cửa sổ hệ điều hành hoạt động trơn tru, không bị crash ứng dụng.
\end{itemize}

\subsection{Kiểm thử phần tính năng chính}
\begin{itemize}
    \item \textbf{Test v1 gameCore/app.cpp: Tạo layout.h và build file thực thi .exe, đảm bảo màn hình 9:16 trước khi viết code tiếp.} \\
    \textbf{Mô tả:} Test này kiểm tra việc thiết lập layout cơ bản và build ứng dụng, đảm bảo tỷ lệ màn hình 9:16 để phù hợp với thiết kế game. Layout.h chứa các định nghĩa về kích thước cửa sổ, vị trí các thành phần UI. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Tạo file layout.h với các macro định nghĩa WIDTH = 900, HEIGHT = 1600 hoặc tương tự để đạt tỷ lệ 9:16.
        \item Build ứng dụng bằng lệnh g++ hoặc CMake, tạo file .exe.
        \item Chạy ứng dụng và kiểm tra tỷ lệ màn hình.
        \item Thay đổi độ phân giải màn hình và chạy lại để xác nhận responsive.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu cửa sổ hiển thị đúng 9:16, build thành công không lỗi. Fail nếu tỷ lệ sai hoặc crash khi khởi chạy. \\
    \textbf{Trường hợp biên:} Test trên màn hình nhỏ (480p), lớn (4K), và tỷ lệ khác (16:9) để đảm bảo fallback.
    \item \textbf{Test v1 gameCore/app.cpp: Tạo 1 trong 6 khối (L,I,T,Z left, Z right, O) đơn sắc.} \\
    \textbf{Mô tả:} Test kiểm tra việc tạo và hiển thị một trong sáu loại khối Tetris cơ bản với màu đơn sắc. Khối phải có hình dạng đúng và màu đồng nhất. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi tạo ứng dụng và tạo một khối ngẫu nhiên từ 6 loại.
        \item Quan sát hình dạng và màu sắc của khối trên màn hình.
        \item Kiểm tra với từng loại khối riêng biệt.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu khối hiển thị đúng hình dạng và màu đơn sắc. Fail nếu hình dạng sai hoặc màu không đồng nhất.
    \item \textbf{Test v1 gameCore/app.cpp: Đổ màu ngẫu nhiên (Blue, Red, Green, Yellow, Orange, Purple).} \\
    \textbf{Mô tả:} Test kiểm tra việc gán màu ngẫu nhiên cho khối từ danh sách màu được chỉ định. Mỗi khối phải có màu khác nhau khi tạo mới. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Tạo nhiều khối liên tiếp và quan sát màu sắc.
        \item Đảm bảo màu nằm trong danh sách: Blue, Red, Green, Yellow, Orange, Purple.
        \item Kiểm tra phân phối ngẫu nhiên.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu màu ngẫu nhiên và trong danh sách. Fail nếu màu cố định hoặc ngoài danh sách.
    \item \textbf{Test v1 gameCore/app.cpp: Cho khối rơi tự do ở 1 vị trí bất kỳ (on top).} \\
    \textbf{Mô tả:} Test kiểm tra khối rơi tự do từ vị trí trên cùng xuống dưới theo thời gian. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Khởi tạo khối ở vị trí trên cùng.
        \item Quan sát khối rơi xuống tự do mà không can thiệp.
        \item Kiểm tra tốc độ rơi đều đặn.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu khối rơi mượt mà từ trên xuống. Fail nếu không rơi hoặc rơi không đều.
    \item \textbf{Test v1 gameCore/app.cpp: Điều khiển khối bằng các mũi tên và các phím chữ WASD.} \\
    \textbf{Mô tả:} Test kiểm tra khả năng điều khiển khối di chuyển trái/phải và xoay bằng phím mũi tên và WASD. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Nhấn phím mũi tên trái/phải hoặc A/D để di chuyển khối.
        \item Nhấn mũi tên lên hoặc W để xoay khối.
        \item Kiểm tra phản hồi tức thì.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu khối di chuyển và xoay đúng. Fail nếu không phản hồi hoặc sai hướng.
    \item \textbf{Test v1 gameCore/app.cpp: Hiệu ứng chạm và dừng rơi khối.} \\
    \textbf{Mô tả:} Test kiểm tra khối dừng rơi khi chạm đáy hoặc khối khác, và hiệu ứng trực quan. \\
    \textbf{Kịch bản kiểm thử:} 
    \begin{enumerate}
        \item Để khối rơi đến đáy hoặc trên khối đã dừng.
        \item Quan sát khối dừng và hiệu ứng (nếu có).
        \item Kiểm tra không rơi thêm.
    \end{enumerate}
    \textbf{Tiêu chí pass/fail:} Pass nếu khối dừng đúng lúc và hiệu ứng rõ. Fail nếu tiếp tục rơi hoặc không hiệu ứng.
    \item \textbf{Test Input Lưới:} Truyền input qua Terminal (W/A/S/D) để kiểm tra luồng di chuyển trên lưới.
    \item \textbf{Test Xóa Hàng:} Nhét mock-data (1 hàng đầy) vào ma trận $\rightarrow$ Nhập lệnh cập nhật vòng lặp $\rightarrow$ Expected: Hàng bị xóa, trả về điểm số tương ứng.
    \item \textbf{Test removeLine:} Thiết lập một hàng đầy trong bảng, gọi hàm \texttt{removeLine()} và kiểm tra trạng thái ma trận sau khi xoá. $\rightarrow$ Expected: dòng đầy biến mất, các dòng phía trên trượt xuống một vị trí, dòng đầu tiên trên cùng được điền bằng khoảng trắng và các cạnh tường `#` không thay đổi.
    \item \textbf{Test tốc độ rơi tăng sau removeLine:} Thực hiện một hoặc nhiều lần xoá hàng đầy và quan sát tốc độ rơi của block tiếp theo. $\rightarrow$ Expected: sau mỗi lần gọi \texttt{removeLine()} thành công, tốc độ rơi tăng nhanh hơn so với tốc độ trước đó.
    \item \textbf{Test giao diện viền và block vuông:} Kiểm tra hiển thị khung màn hình và khối rơi ở chế độ GUI/console, đảm bảo mỗi ô có tỉ lệ 1:1, không bị dãn méo thành hình chữ nhật. $\rightarrow$ Expected: khung và block hiển thị đồng nhất, các ô vuông, góc vuông rõ ràng và không có hiện tượng kéo dãn bất thường.
    \item \textbf{Test Xoay Block:} Đặt block vào nhiều vị trí khác nhau, bao gồm vị trí trung tâm, sát tường và sát đáy, sau đó nhấn phím xoay. $\rightarrow$ Expected: block xoay đúng hình dạng mong đợi, không bị biến dạng, không chồng lên biên và không vượt quá ranh giới ma trận.
    \item \textbf{Test Xoay Chặn (Wall Kick):} Đặt khối sát tường (biên ma trận) $\rightarrow$ Nhấn lệnh Xoay $\rightarrow$ Expected: Khối không bị xoay vượt ra ngoài index của ma trận lưới.
\end{itemize}

\subsection{Mô tả kỹ thuật chi tiết kiểm thử chức năng Game Core}

Kiểm thử chức năng Game Core phải tập trung vào việc xác nhận toàn bộ luồng xử lý ma trận, đặc biệt là thuật toán loại bỏ dòng đầy trong hàm \texttt{removeLine()}. Với bản mã hiện tại, bảng chơi được lưu trong mảng hai chiều \texttt{board[H][W]}; hàng trên cùng và hàng dưới cùng, cùng hai cột ngoài cùng, được dùng làm biên tường cố định chứa ký tự \texttt{'#'}. Các ô nội bộ có thể chứa khoảng trắng hoặc ký tự khối gạch. Vì vậy, test cần đảm bảo rằng khi một hàng chơi được xác định là đầy, thuật toán chỉ thay đổi chính hàng đó và các hàng phía trên chứ không can thiệp vào biên tường hay phát sinh ký tự lạ.

Mục tiêu kiểm thử bao gồm các khía cạnh sau:

\begin{itemize}
    \item Xác thực độ chính xác của nhận biết hàng đầy: mọi ô trong hàng phải khác khoảng trắng, không kể biên tường hai bên. Hàng được coi là đầy chỉ khi tất cả vị trí có thể chứa khối nội bộ đều bị lấp đầy.
    \item Xác nhận hành vi dịch chuyển: khi một hàng đầy bị xóa, tất cả các hàng phía trên nó phải trượt xuống đúng một vị trí, trong khi hàng trên cùng được lấp đầy bằng ô trống. Việc dịch chuyển phải bảo toàn trạng thái biên tường, tức không có ký tự khác thay thế cho ký tự \texttt{'#'} ở viền.
    \item Kiểm tra hiệu ứng nhiều hàng liên tiếp: khi có hai hoặc nhiều hàng đầy trong một lượt, hàm cần xoá từng hàng đúng thứ tự từ dưới lên trên và giữ nguyên thứ tự tương đối của các hàng phía trên.
    \item Đảm bảo không thay đổi khối đang rơi: hàm \texttt{removeLine()} chỉ được gọi sau khi khối đã được ghi vào bảng; do đó test phải đảm bảo rằng trạng thái khối vừa đặt không bị ảnh hưởng hoặc bị xoá sai khi xử lý hàng đầy.
    \item Xác minh khả năng chịu lỗi với điều kiện biên: hàng đầy ở hàng gần đáy nhất, ở hàng cạnh trên hoặc khi bảng chỉ còn lại một hàng trống trên cùng phải được xử lý đúng.
\end{itemize}

Kiểm thử chi tiết cần được thực hiện thông qua cả hai phương thức sau:

\begin{enumerate}
    \item \textbf{Kiểm thử thủ công dựa trên console:} bằng cách dựng bảng mẫu và quan sát kết quả của hàm \texttt{removeLine()} sau mỗi lần gọi. Đây là cách phù hợp khi chưa có hạ tầng test tự động, giúp đánh giá trực quan mã thực thi có khớp với luồng trò chơi hay không.
    \item \textbf{Kiểm thử tự động lý thuyết:} xây dựng trường hợp kiểm thử giả lập với dữ liệu bảng trong file test, gọi trực tiếp \texttt{removeLine()} và so sánh trạng thái ma trận với dữ liệu mong đợi. Nếu tương lai mở rộng thêm framework unit test, đây sẽ là test thích hợp nhất để bảo đảm tính bền vững của hàm.
\end{enumerate}

\paragraph{Mô tả chi tiết kịch bản kiểm thử \texttt{removeLine()}}

1. \textbf{Khởi tạo môi trường test:}
    \begin{itemize}
        \item Tạo bảng chơi mới bằng hàm \texttt{initBoard()}. Kết quả ban đầu phải có biên \texttt{'#'} cố định ở hàng đầu tiên, hàng cuối cùng, cột trái và cột phải.
        \item Gán giá trị char cho các ô trong một hàng nội bộ (không tính biên) thành ký tự gạch như \texttt{'I'}, \texttt{'T'} hoặc \texttt{'O'} để tạo thành một hàng đầy. Các hàng khác phía trên hoặc phía dưới có thể giữ nguyên trạng thái ban đầu hoặc được gán giá trị động để kiểm tra dịch chuyển.
    \end{itemize}

2. \textbf{Thiết lập dữ liệu kiểm thử:}
    \begin{itemize}
        \item Trường hợp cơ bản: điền đầy toàn bộ một hàng nội bộ ở vị trí giữa bảng (ví dụ hàng \texttt{i = H-2}). Phía trên hàng này, thiết lập một số ký tự giả định trong hàng tiếp theo để quan sát hiệu quả trượt.
        \item Trường hợp đa dòng: điền đầy hai hoặc ba hàng liên tiếp phía dưới, đảm bảo \texttt{removeLine()} có thể phát hiện và xử lý từng hàng theo thứ tự từ dưới lên.
        \item Trường hợp giáp cạnh: điền đầy hàng ngay trên cạnh đáy bảng (không tính biên cuối) và hàng ngay dưới cạnh trên cùng để kiểm tra công thức so sánh ô trống và phần biên tường.
        \item Trường hợp không có dòng đầy: gọi \texttt{removeLine()} khi bảng chỉ chứa các ô trống bên trong và biên tường. Kết quả mong đợi là bảng giữ nguyên nguyên vẹn.
    \end{itemize}

3. \textbf{Thực thi hàm và quan sát kết quả:}
    \begin{itemize}
        \item Gọi \texttt{removeLine()} một lần sau khi đã dựng xong bảng mẫu.
        \item Quan sát dữ liệu bảng bằng cách in ra console hoặc so sánh biểu diễn ký tự của bảng với một bảng kỳ vọng đã chuẩn bị sẵn.
        \item Với từng hàng đầy đã tạo, xác nhận rằng nó biến mất hoàn toàn, không để lại ký tự gạch nào trên hàng đó.
        \item Với mỗi hàng phía trên, xác nhận rằng nội dung ban đầu của hàng phía trên đó đã chuyển xuống đúng một vị trí bên dưới.
        \item Dòng đầu tiên trong nội bộ sau khi xử lý phải trở thành hoàn toàn khoảng trắng, ngoại trừ biên tường giữ nguyên \texttt{'#'}. Nếu dòng này không được điền đúng, thuật toán đã dịch chuyển sai chiều hoặc quên khởi tạo hàng trên cùng.
    \end{itemize}

4. \textbf{Xác minh biên tường và trạng thái lạ:}
    \begin{itemize}
        \item Kiểm tra rằng các ký tự \texttt{'#'} ở cột trái và cột phải không bị ghi đè, cả ở các hàng vừa dịch chuyển và ở hàng trên cùng.
        \item Kiểm tra tất cả các ô nội bộ để đảm bảo không còn ký tự lạ như ký tự ASCII không in được hoặc số không mong muốn.
        \item Kiểm tra rằng các ô trống dùng ký tự khoảng trắng \texttt{' '} chứ không sử dụng giá trị rác hay ký tự null.
    \end{itemize}

5. \textbf{Đánh giá hiệu suất và tính ổn định:}
    \begin{itemize}
        \item Hàm \texttt{removeLine()} hiện tại duyệt từ dưới lên trên và sau khi xoá một hàng sẽ tăng chỉ số \texttt{i} để kiểm tra lại hàng mới được dịch chuyển xuống. Test phải xác minh logic này không bỏ sót hàng đầy liên tiếp. Nếu tồn tại hai hàng đầy liên tiếp, việc tăng \texttt{i} trở lại sau khi xoá sẽ tiếp tục kiểm tra hàng vừa kéo xuống.
        \item Kiểm tra thêm với bảng có nhiều dòng đầy rải rác để xác thực không có hành vi không mong muốn khi một dòng đầy mới được tạo sau khi các hàng phía trên trượt xuống.
    \end{itemize}

\subsubsection*{Kịch bản kiểm thử chi tiết cho chức năng removeLine}

Để mô tả chi tiết hơn, test case mẫu có thể bao gồm các bước sau đây:

\begin{enumerate}
    \item Khởi tạo bảng mẫu với kích thước chuẩn \texttt{H = 20}, \texttt{W = 15}.
    \item Thiết lập hàng \texttt{i = 17} (hàng thứ hai tính từ đáy) với giá trị đầy đủ: mọi cột từ \texttt{j = 1} đến \texttt{j = W-2} đều chứa ký tự ghép khối.
    \item Thiết lập hàng \texttt{i = 16} phía trên với một mẫu không đầy, ví dụ ô \texttt{j = 2} chứa \texttt{'I'}, ô \texttt{j = 4} chứa \texttt{'T'}, các ô còn lại là khoảng trắng, để kiểm tra dịch chuyển chính xác.
    \item Gọi \texttt{removeLine()}. Sau khi hàm chạy hoàn tất, xác minh các điều kiện sau:
        \begin{itemize}
            \item Hàng \texttt{i = 17} phải trở thành một hàng trống hoàn chỉnh, ngoại trừ biên tường hai bên.
            \item Nội dung ban đầu của hàng \texttt{i = 16} phải nằm ở hàng \texttt{i = 17} sau khi dịch chuyển.
            \item Hàng \texttt{i = 16} phải trở thành một hàng trống bên trong (ngoại trừ hai ô biên), vì các hàng phía trên tiếp tục dịch xuống.
            \item Các dòng biên \texttt{i = 0} và \texttt{i = H-1} vẫn phải chứa ký tự \texttt{'#'} ở toàn bộ các vị trí biên, không bị biến đổi.
        \end{itemize}
    \item Lặp lại test với hai hàng đầy liên tiếp ở \texttt{i = 17} và \texttt{i = 16}. Kỳ vọng: cả hai hàng đều bị xoá và hàng \texttt{i = 15} dịch xuống tới hàng \texttt{i = 17}, sau đó hàng \texttt{i = 16} cũng trở thành trống.
    \item Thực hiện kiểm thử biên với hàng đầy tại \texttt{i = 1} (gần biên trên cùng). Sau khi hàm chạy, hàng tiếp theo phải dịch xuống chính xác và hàng \texttt{i = 1} trở thành trống.
    \item Thực hiện kiểm thử trường hợp không có hàng đầy, xác nhận bảng không đổi để tránh lỗi xóa sai.
\end{enumerate}

\subsubsection*{Những điều cần kiểm tra khi xây dựng test tự động}

\begin{itemize}
    \item Tạo một hàm hỗ trợ riêng trong file test để khởi tạo ma trận mẫu và chuyển đổi nó thành chuỗi văn bản dễ so sánh.
    \item So sánh toàn bộ bảng sau khi gọi \texttt{removeLine()} với một bảng kỳ vọng đã chuẩn bị sẵn, thay vì chỉ kiểm tra một vài ô cục bộ.
    \item Nếu codebase mở rộng, cân nhắc tạo thêm hàm \texttt{isLineFull(int row)} để tách riêng việc xác định hàng đầy, giúp kiểm thử unit dễ dàng hơn.
    \item Phân tách các trường hợp: test "đơn" (single line clear), test "dồn" (multiple line clear), test "giáp đáy" và test "giáp trên".
\end{itemize}

\subsubsection*{Các chỉ tiêu kiểm thử số lượng và chất lượng}

Bên cạnh việc kiểm tra đúng / sai, cần đánh giá thêm các tiêu chí sau:

\begin{itemize}
    \item \textbf{Tính lặp lại}: kết quả test phải nhất quán khi chạy nhiều lần với cùng dữ liệu input.
    \item \textbf{Tính độc lập}: test \texttt{removeLine()} nên không phụ thuộc vào trạng thái khối rơi hiện tại; nếu cần, khối đang rơi có thể được gỡ khỏi bảng trước khi test.
    \item \textbf{Tính minh bạch}: kết quả kỳ vọng được mô tả rõ ràng, có thể đối chiếu bằng cách so sánh từng dòng ASCII, thay vì đánh giá cảm quan.
    \item \textbf{Hiệu suất}: hàm cần xử lý bảng kích thước 20x15 trong vòng thời gian ngắn, vì nó được gọi mỗi khi một khối cố định một lần. Mặc dù test không cần đo thời gian chính xác, nhưng nên chú ý đến việc không dùng vòng lặp vô hạn hoặc xử lý thừa phức tạp.
\end{itemize}

\subsection{Mô tả kiểm thử tốc độ rơi tăng nhanh sau removeLine}

Phần kiểm thử này tập trung vào việc xác nhận cơ chế tăng tốc độ rơi mỗi khi một hay nhiều hàng được xoá thành công bằng hàm \texttt{removeLine()}. Trong gameplay Tetris, đây là một cảnh báo lối chơi quan trọng để tăng độ khó và tạo động lực cho người chơi. Test cần kiểm tra rằng tốc độ rơi khối sau khi xoá dòng đầy sẽ thực sự nhanh hơn so với tốc độ ban đầu và tốc độ trước đó.

Các trường hợp kiểm thử đề xuất:

\begin{itemize}
    \item Bắt đầu với tốc độ rơi mặc định, sau đó xoá một hàng đầy và đo tốc độ rơi mới của block hiện tại hoặc block kế tiếp.
    \item Thực hiện nhiều lần xoá hàng đầy liên tiếp, kiểm tra xem tốc độ có tăng dần theo từng lần xoá hay không.
    \item Kiểm tra điều kiện không xoá được hàng đầy: nếu \texttt{removeLine()} không được gọi, tốc độ rơi không thay đổi.
    \item Kiểm tra rằng tốc độ tăng không vượt quá mức tối đa cho phép hoặc gây lỗi khi giá trị tốc độ trở nên quá nhỏ.
\end{itemize}

Kịch bản kiểm thử chi tiết:

\begin{enumerate}
    \item Thiết lập trò chơi ở tốc độ rơi mặc định. Xác định giá trị thời gian trễ mỗi lần rơi ban đầu (ví dụ 300ms).
    \item Tạo dữ liệu sao cho ít nhất một hàng đầy sẽ được xoá bởi hàm \texttt{removeLine()}.
    \item Gọi \texttt{removeLine()} và ghi lại tốc độ rơi ngay sau khi xoá. So sánh với giá trị tốc độ trước đó.
    \    
    \item Lặp lại với hai hoặc ba lần xoá hàng đầy liên tiếp để kiểm tra tốc độ có tiếp tục tăng dần một cách hợp lý.
    \    
    \item Thực hiện kiểm thử khi không có hàng đầy, xác nhận tốc độ rơi không thay đổi và hàm không làm tăng nhầm tốc độ.
\end{enumerate}

Tiêu chí pass/fail:

\begin{itemize}
    \item Pass: sau mỗi lần xoá thành công bằng \texttt{removeLine()}, thời gian giữa hai lần rơi liên tiếp giảm, biểu thị tốc độ rơi tăng lên.
    \item Fail: tốc độ rơi giữ nguyên hoặc giảm sau khi xoá dòng, hoặc tốc độ tăng vượt quá giới hạn vận hành bình thường.
\end{itemize}

\subsection{Mô tả kiểm thử giao diện viền và block vuông}

Phần kiểm thử này tập trung vào hiển thị cấu trúc lưới và các block rơi sao cho mỗi ô vuông, không bị kéo giãn thành hình chữ nhật. Với hiện trạng hình chữ nhật (HCN) ở viền và block, test cần xác định liệu các đối tượng trên lưới có giữ tỉ lệ 1:1 ở cả chiều ngang và chiều dọc hay không. Test còn phải kiểm tra góc kết nối giữa viền và ô block để đảm bảo các góc vuông sắc nét, không bị bo hoặc kéo dài.

\subsection{Mô tả kiểm thử chức năng xoay block}

Kiểm thử xoay block là một phần quan trọng của module Game Core vì nó xác định tính chính xác của cơ chế biến đổi hình dạng khối khi nhận lệnh người chơi. Mục tiêu của phần này là kiểm tra chắc chắn rằng, sau mỗi lần xoay, block giữ nguyên kích thước hợp lý, không bị ảnh hưởng bởi vị trí xung quanh và không vượt ra ngoài ranh giới ma trận hay chồng chéo với biên tường.

Các yếu tố kiểm thử chính:

\begin{itemize}
    \item Xác thực xoay ở vị trí trung tâm: đặt block vào vị trí trung tâm của bảng và xoay khối nhiều lần, kiểm tra xem hình dạng của block có đổi đúng theo quy tắc xoay mong đợi.
    \item Xác nhận xoay sát tường: đặt block sát tường trái, phải và sát đáy, sau đó xoay. Kiểm tra xem block có bị cắt mất phần đầu, không chạm vào biên tường và không vượt ra khỏi ma trận.
    \item Xác minh xoay ở gần block khác: nếu hiện trạng có các ô đã cố định bên cạnh block đang xoay, kiểm tra xem xoay không làm khối chồng lên ô đã có.
    \item Kiểm tra tính ổn định sau nhiều xoay: thực hiện chuỗi xoay liên tiếp (ví dụ 4 lần) để đảm bảo block trở lại hình dạng ban đầu hoặc dạng chuyển tiếp hợp lệ mà không lỗi hình học.
\end{itemize}

Kịch bản kiểm thử xoay block nên bao gồm:

\begin{enumerate}
    \item Thiết lập bảng với một block ở vị trí trung tâm. Xoay block và kiểm tra hình dạng, số lượng ô block và chuyển động của các ô có đúng không.
    \item Đặt block sát tường trái, thực hiện xoay, và kiểm tra xem phần block có quá ra khỏi biên hay không. Nếu hệ thống có wall kick, ghi nhận hành vi điều chỉnh vị trí.
    \item Đặt block sát tường phải và sát đáy, xoay mỗi lần để kiểm tra trường hợp góc, cạnh và hình dáng khối không bị sai.
    \item Đặt các khối cố định làm chướng ngại ở gần block xoay, thực hiện xoay và kiểm tra xem block xoay có chuyển vào vị trí hợp lệ hay bị chồng chéo.
\end{enumerate}

Tiêu chí pass/fail:

\begin{itemize}
    \item Pass: block xoay đúng hình dạng, không bị biến dạng, không chồng lên biên hoặc vùng đã được cố định, và vẫn nằm trong giới hạn ma trận.
    \item Fail: block bị cắt mất, chồng chéo với biên tường hoặc phần tử đã được cố định, hoặc không xoay đúng hình dạng mong đợi.
\end{itemize}

\subsubsection*{Lưu ý khi kiểm thử}

\begin{itemize}
    \item Nếu game implement wall kick, kiểm tra rằng block có thể điều chỉnh vị trí nhỏ để vừa vặn khi xoay sát biên.
    \item Ghi lại phím xoay sử dụng trong test và đảm bảo input map đúng với hành vi game hiện tại.
    \item Với mỗi lần xoay, kiểm tra cả trạng thái trước và sau xoay để xác định sai sót ở bước chuyển đổi.
\end{itemize}

\subsubsection*{Kết nối với các test khác}

Phần test xoay block nên được chạy song song với test removeLine và test giao diện block vuông, vì việc xoay có thể ảnh hưởng trực quan đến vị trí hiển thị và trạng thái bảng sau khi block cố định.

\subsection{Mô tả kiểm thử giao diện viền và block vuông}

Các bước kiểm thử đề xuất:

\begin{enumerate}
    \item Chạy ứng dụng ở chế độ hiện tại và quan sát khung hiển thị cùng các block trong khu vực chơi.
    
    \item Xác thực từng ô trong lưới có kích thước bằng nhau theo cả chiều ngang và chiều dọc. Nếu sử dụng GUI, kiểm tra thước đo pixel hoặc lưới hỗ trợ để đảm bảo tỷ lệ là chính xác. Nếu sử dụng console, kiểm tra font chữ và cỡ ký tự để đảm bảo ô hiển thị vuông.
    
    \item Kiểm tra viền màn hình: đường biên ngoài cùng phải tạo thành hình chữ nhật cân đối với các góc 90 độ và cạnh dọc/ngang có cùng tỷ lệ so với lưới nội bộ.
    
    \item Kiểm tra khối: mỗi block phải được hiển thị trong các ô vuông đồng nhất. Các phần tử block lân cận nên có cùng chiều cao và chiều rộng, không bị kéo dài theo một chiều.
    
    \item Kiểm tra điều kiện thay đổi kích thước: nếu ứng dụng hỗ trợ thay đổi kích thước cửa sổ hoặc chế độ màn hình khác nhau, đảm bảo hiển thị vẫn duy trì ô vuông trên mọi kích thước.
    
    \item Kiểm tra mô tả trực quan: ghi nhận nếu viền hoặc block xuất hiện như hình chữ nhật dẹt/chữ nhật kéo dài. Nếu phát hiện HCN, note cụ thể vị trí và trạng thái điều kiện để sửa.
\end{enumerate}

\paragraph{Tiêu chí pass/fail}

\begin{itemize}
    \item Pass: tất cả ô lưới và block hiển thị với tỉ lệ 1:1, viền rõ ràng, góc vuông sắc nét và không có biến dạng HCN.
    \item Fail: có sự khác biệt rõ ràng giữa chiều ngang và chiều dọc của các ô/block, viền bị kéo dài hoặc không tạo góc vuông chính xác.
\end{itemize}

\subsubsection*{Khuyến nghị kiểm thử mở rộng}

\begin{itemize}
    \item Thực hiện kiểm thử trên tối thiểu hai chế độ hiển thị khác nhau nếu có: console và GUI hoặc các độ phân giải khác nhau.
    \item Nếu game dùng bộ đếm pixel cố định, kiểm tra rằng hệ số tỷ lệ của khung vẽ luôn giữ 1:1 cho từng ô.
    \item Kiểm tra phím điều khiển hoặc tùy chọn cấu hình hiển thị nếu có, đảm bảo các thay đổi vẫn giữ cho mỗi ô vuông.
\end{itemize}

\subsubsection*{Liên hệ với nâng cấp giao diện}

Đánh giá góc hiển thị viền và block vuông có thể liên quan trực tiếp tới việc tái thiết kế khung hiển thị hoặc chỉnh lại chiều cao/chiều rộng của đối tượng render. Do vậy, phần test này nên được chạy cùng với phát triển giao diện và tích hợp vào bản demo hình dạng block mới.

\subsubsection*{Tại sao test giao diện viền và block vuông quan trọng}

Giao diện lưới vuông giúp game cảm nhận đúng tỷ lệ Tetris, tránh tình trạng khung và block bị méo mó làm giảm độ chính xác chơi và cảm giác thị giác. Phần kiểm thử này giúp đảm bảo rằng các nguyên tắc hình học của lưới được duy trì, tạo ra trải nghiệm trực quan đồng đều và chuyên nghiệp hơn.

\subsubsection*{Kết luận}

Phần kiểm thử giao diện viền và block vuông phải được cộng vào danh sách kiểm thử Module Game Core/Giao diện vì đây là một lỗi trực quan lõi, ảnh hưởng tới trải nghiệm người chơi ngay lập tức. Mô tả kiểm thử nên ghi lại rõ ràng các điều kiện khởi tạo, bước quan sát và tiêu chí pass/fail.
Trong logic Tetris, loại bỏ hàng đầy là một hành vi lõi quyết định trải nghiệm người chơi. Nếu hàm này hoạt động sai, hậu quả có thể là:

\begin{itemize}
    \item Bảng chơi không thay đổi đúng khi một hàng được xóa, dẫn tới điểm số và trạng thái game sai lệch.
    \item Các khối phía trên không dịch xuống đúng, gây ra lỗi nhảy dòng, tạo ra những ô trống lỗ hoặc xuất hiện khối chồng chéo.
    \item Viền tường bị xóa hoặc ghi đè, khiến bảng bị phá vỡ và game bị crash hoặc chạy sai.
    \item Khi nhiều hàng đầy xảy ra cùng lúc, nếu thuật toán không xử lý theo thứ tự đúng, kết quả có thể mất một hàng hoặc không xóa hàng đúng.
\end{itemize}

Kết luận: phần kiểm thử này nên chiếm trọng tâm trong tài liệu test module Game Core và cần được mô tả kỹ càng. Mục tiêu không chỉ là xác định test case mà còn là giải thích rõ ràng từng bước thực hiện, điều kiện đầu vào, kết quả mong đợi và cách kiểm tra từng điểm quan trọng.

\label{LastPageTestList}
\end{document}