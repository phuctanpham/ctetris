% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode
\documentclass[12pt,a4paper]{article}

\IfFileExists{src/libs/body.tex}{\input{src/libs/body.tex}}{\input{../libs/body.tex}}
\IfFileExists{src/libs/header.tex}{\input{src/libs/header.tex}}{\input{../libs/header.tex}}
\IfFileExists{src/libs/footer.tex}{\input{src/libs/footer.tex}}{\input{../libs/footer.tex}}
\usepackage{docmute}
\hypersetup{pdftitle={Báo cáo Kỹ năng nghề nghiệp - tính năng cốt truyện: Đặc tả thiết kế}}

\begin{document}
\providecommand{\StandaloneSectionSetup}[2]{\ApplyStandardFooter{#1}{#2}}
\StandaloneSectionSetup{05-gameStoryEngineering.tex}{LastPageGameStoryEngineering}
\onehalfspacing
\section{Đặc tả kỹ thuật tính năng cốt truyện}

\subsection{Sơ đồ luồng (Flow/State Machine)}
// tự viết thêm: Sơ đồ sequence khi init module: Load file -> Parse JSON -> Đẩy vào Queue -> Render frame by frame.

\subsection{Cấu trúc dữ liệu (Data Structures)}
// tự viết thêm: Định nghĩa cấu trúc file JSON nội bộ. Ví dụ struct trên code: `struct DialogNode { int id; std::string speakerName; std::string content; std::string spritePath; }`

\subsection{Giao diện giao tiếp (Public interface - Expose cho main.cpp)}
// tự viết thêm: Liệt kê các hàm nằm trong src/gameStory/app/include. VD: 
// tự viết thêm: `bool Story::Initialize(const std::string\& scriptPath);`
// tự viết thêm: `void Story::Update(float deltaTime);`
// tự viết thêm: `bool Story::IsFinished() const;`

\subsection{Thư viện phụ thuộc (Dependencies)}
// tự viết thêm: Ghi rõ các thư viện C++ ngoài dùng cho module này (VD: `nlohmann/json` để parse data).

\label{LastPageGameStoryEngineering}
\end{document}